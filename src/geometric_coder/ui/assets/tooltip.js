window.dccFunctions = window.dccFunctions || {};
window.dccFunctions.formatSimilarity = function (value) {
  return Number(value).toFixed(3);
};
window.dccFunctions.formatProbability = function (value) {
  return Number(value).toFixed(6);
};
